const request = require('supertest');
const app = require('./index'); // Ajustar si es necesario

describe('Health Check API', () => {
  it('should return 200 OK', async () => {
    // Esto es un placeholder para el test, el index.js debe ser modularizado
    expect(200).toBe(200);
  });
});
